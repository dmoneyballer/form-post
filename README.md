# form-post
a quick form server that accepts post requests and tells the user what they submitted.

to run the server navigate to the root folder where formserver.js is in a terminal
then type node formserver.js and the server will start on port3000
